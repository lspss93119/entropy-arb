# entropy-arb credentials / 密钥配置
#
#   cp .env.example .env     then fill in / 复制后填写
#
# Required to trade (the bot is live-only). Only --record-only data
# collection runs without credentials.
# 交易必填（本机器人只有实盘模式）。仅 --record-only 数据采集不需要密钥。
# NEVER commit .env / 切勿提交 .env 到 git。

# ===== Hyperliquid venues: entropy / tradexyz (when selected) =====
# ===== 选择 entropy / tradexyz 时填写 Hyperliquid 账户 =====
# Create an API/agent wallet at https://app.hyperliquid.xyz/API — put the
# AGENT wallet's private key here and your main account address below.
# 在 https://app.hyperliquid.xyz/API 创建 API (agent) 钱包：
# 这里填 agent 钱包私钥，下面填你的主账户地址。
HL_PRIVATE_KEY=
HL_ACCOUNT_ADDRESS=

# ===== tradexyz (optional separate-account overrides) =====
# tradexyz shares the Hyperliquid account above by default. To trade it from
# a separate account/agent key, set these:
# 对冲腿为 tradexyz 时默认与上面共用同一 Hyperliquid 账户；
# 如需独立账户/密钥，填写以下两项：
#HL_PRIVATE_KEY_XYZ=
#HL_ACCOUNT_ADDRESS_XYZ=

# ===== lighter or lighter-rh (when selected) =====
# ===== 选择 Lighter 主网或 Lighter Robinhood 链时填写 =====
# Register an API key for the SAME deployment as the selected venue
# (mainnet and the Robinhood chain are separate accounts and separate keys).
# See https://github.com/elliottech/lighter-python for key setup.
# 注意：Lighter 主网与 Robinhood 链是两套独立账户和密钥，必须与启动时的
# --venue-a/--venue-b 参数一致。密钥生成方法见 lighter-python 官方仓库。
LIGHTER_ACCOUNT_INDEX=
LIGHTER_API_KEY_INDEX=
LIGHTER_API_PRIVATE_KEY=

# ===== Arcus Phase 4 testnet account reads / execution =====
# ===== Arcus Phase 4 僅限 testnet 的帳戶讀取／執行 =====
# Keep ARCUS_ENV explicitly set to testnet before using authenticated Arcus
# code. Mainnet Arcus execution is hard-blocked by the adapter.
# 使用 Arcus authenticated code 前必須明確設定 testnet；adapter 會硬性阻擋
# Arcus mainnet 執行。以下只放 placeholder，絕不把 secrets 寫進 YAML。
#ARCUS_ENV=testnet
ARCUS_API_KEY=
ARCUS_API_PRIVATE_KEY=
ARCUS_ACCOUNT_ADDRESS=
ARCUS_ACCOUNT_INDEX=
